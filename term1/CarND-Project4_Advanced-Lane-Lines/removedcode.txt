from slidingwindow import sliding_window_search
#warped_img = cv2.cvtColor(warped_img,cv2.COLOR_RGB2GRAY)
warped_img_lanes = sliding_window_search(warped_img, do_plot=False)

f, (ax1,ax2) = plt.subplots(1, 2, figsize=(12, 5))
f.tight_layout()
ax1.imshow(warped_img_lanes)
ax2.imshow(warped_img)
print(warped_img.shape)
print (warped_img.dtype)
print (warped_img.size)
print( )
print(warped_img_lanes.shape)
print (warped_img_lanes.dtype)
print (warped_img_lanes.size)
#plt.imshow(warped_img)
#plt.show()
#curved_warped_img2_lanes = sliding_window_search(curved_warped_img2, window_height=180, do_plot=True)
#curved_warped_img3_lanes = sliding_window_search(curved_warped_img3, window_height=180, do_plot=True)
#curved_warped_img1_lanes = sliding_window_search(curved_warped_img1, window_height=180, do_plot=True)